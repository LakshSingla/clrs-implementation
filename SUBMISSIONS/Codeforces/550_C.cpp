#include <iostream>
#include <vector>

struct last_3_ret {
	int a, b, c;
};

last_3_ret last_3(const std::vector<bool>&v){
	const x = v.size()-1;
	while
	return
}

bool div_by_8(char a, char b, char c) {
	int ia = static_cast<int>(a);
	int ib = static_cast<int>(b);
	int ic = static_cast<int>(c);
	int n = 100*ia + 10*ib + ic;
	return !(n % 8);
}


int main(){
	char c;
	std::vector<char> inp ;
	while(std::cin >> c)
		inp.push_back(c);
	std::vector<bool>is_present(inp.size(), true);
	//for(const auto&elem : is_present) std::cout << elem;
	
	while(true){
		if()	
	}
	
	std::cout << std::endl;
}
